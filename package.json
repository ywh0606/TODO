{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "简洁好用的桌面清单工具",
  "main": "electron/main.js",
  "scripts": {
    "dev": "bash -c 'unset ELECTRON_RUN_AS_NODE && vite & sleep 3 && electron .'",
    "build": "vite build",
    "electron:build": "vite build && electron-builder"
  },
  "dependencies": {
    "dayjs": "^1.11.0",
    "pinia": "^2.1.0",
    "uuid": "^9.0.0",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "electron": "28.3.3",
    "electron-builder": "^24.0.0",
    "vite": "^5.0.0"
  }
}
